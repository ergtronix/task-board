# CLAUDE.md — task-board

## プロジェクト概要

タスクボード管理アプリケーション。

---

## Git 運用ルール

### 基本方針
- **コードを変更するたびに GitHub へプッシュすること。**
- コミットとプッシュはセットで行う。変更をローカルに溜めない。

### コミット手順
1. 変更ファイルを確認: `git status`
2. 関連ファイルを個別にステージ: `git add <file>` （`git add .` は機密ファイル混入防止のため避ける）
3. コミット: `git commit -m "簡潔なメッセージ"`
4. プッシュ: `git push origin main`

### コミットメッセージ規則
- 日本語・英語どちらでもよい
- 内容を端的に表す1行メッセージ
- Co-authored-by フッターを付与する場合は HEREDOC 形式を使う

### ブランチ
- 通常作業は `main` ブランチで行う
- 大きな機能追加・破壊的変更は feature ブランチを切って PR を作成する

### 禁止事項
- `--force` プッシュは原則禁止（ユーザーが明示的に指示した場合のみ）
- `--no-verify` によるフック回避は禁止
- `.env` や認証情報をコミットしない

---

## リモートリポジトリ

- **GitHub**: https://github.com/ergtronix/task-board
- デフォルトブランチ: `main`
