# task-board

タスク管理ボードアプリ。

## 技術スタック

- HTML / CSS / JavaScript（バニラ、フレームワークなし）

## プロジェクト構成

```
task-board/
├── index.html        # エントリーポイント
├── style.css         # スタイルシート
└── script.js         # アプリロジック
```

## 開発ルール

- フレームワークやビルドツールは使わない。ブラウザで直接開ける構成を維持する。
- JavaScriptはES6+で書く（`let`/`const`、アロー関数、テンプレートリテラルを使う）。
- グローバル変数は最小限に抑え、関数スコープやモジュールパターンでまとめる。
- CSSクラス名はBEM風（`board__card`、`board__button--active` など）を推奨。
- コメントは「なぜそうしたか」が自明でない箇所にのみ書く。

## コーディング規約

- インデントはスペース2つ。
- セミコロンあり。
- 文字列はシングルクォート。

## Git運用ルール

- コードを変更するたびに、以下の手順でGitHubにプッシュすること。

```bash
git add .
git commit -m "変更内容を簡潔に記述"
git push
```

- コミットメッセージは日本語でも英語でも可。変更内容が伝わる簡潔な説明を書く。
- 作業単位ごとにコミットする（まとめてプッシュしない）。

## GitHubリポジトリ
https://github.com/ergtronix/SAMURAI_GITHUB

## 作業ディレクトリ
task-board
